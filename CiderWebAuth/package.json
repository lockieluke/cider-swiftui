{
  "name": "cider-web-auth",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.17.12",
    "musickit-typescript": "^1.2.4"
  },
  "dependencies": {
    "await-to-js": "^3.0.0"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --target=safari15 --tree-shaking=true --outfile=dist/index.js",
    "clean": "rm -rf dist/"
  }
}
